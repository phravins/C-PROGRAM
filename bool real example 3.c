#include <stdio.h>
#include <stdbool.h>
int main () {
 bool myage = true;
 bool votingage = false;
 if (myage <= votingage){
 printf("myage is satisfies the voting age");
 } else {
 printf("myage is not satisfies the voting age");
 }
 return 0;
}
