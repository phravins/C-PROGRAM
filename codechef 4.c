#include <stdio.h>

int main() {
    float average_altitude = 35000.750000;
    float maximum_altitude = 45367.098765;
    printf("Average Flight Altitude (float): %f\n", average_altitude);
    printf("Maximum Flight Altitude (double): %f", maximum_altitude);
    return 0;
}
