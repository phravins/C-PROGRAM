#include <stdio.h>
#include <stdbool.h>
int main () {
   bool myage = 18;
   bool votingage = 20;
if(myage >= votingage){
   printf("votingAge is satisfied");
} else {
   printf("votingAge is not satisfied");
}
return 0;
}
